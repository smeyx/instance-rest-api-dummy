{
  "name": "management-rest-dummy",
  "version": "1.0.0",
  "description": "Dummy REST-API for management interface",
  "main": "index.js",
  "scripts": {
    "dev": "tsx watch ./src/server.ts",
    "build": "rimraf ./dist && tsc",
    "serve": "tsx ./src/server.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "pnpm run build && node dist/server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "koa": "^2.14.2",
    "koa-router": "^12.0.0"
  },
  "devDependencies": {
    "@types/koa": "^2.13.6",
    "@types/koa-router": "^7.4.4",
    "rimraf": "^5.0.1",
    "tsx": "^3.12.7",
    "typescript": "^5.1.3"
  }
}
